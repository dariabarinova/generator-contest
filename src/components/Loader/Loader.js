import React from 'react';
// import { connect } from 'react-redux';
// import { Ui } from '../..';
// import { selector as isAppLoaded } from './loaderReducer';

// const decorator = connect(
//   state => ({
//     visible: !isAppLoaded(state),
//   }),
// );

const b = () => (
  <div>
    123
  </div>
);

export default b; // decorator(Ui.Loader);
